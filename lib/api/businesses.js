define(function(require){
  var
    $      = require('jquery')
  , config = require('../../config')
  , utils  = require('../utils')
  ;

  return {
    list: function(query, callback){
      
    }
  };
});